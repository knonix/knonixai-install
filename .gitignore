# Local environment — never commit real credentials
.env
.env.*
!.env.example

# Docker / runtime artifacts
*.log
data/
volumes/
secrets/

# Platform host runtime (operator only — never ship customer data/secrets)
platform/license-service/data/
SESSION_CHECKPOINT.md
